import React from "react"
import { Container } from "./styles";

export default function NavBar() {
    return (
        <Container>
            <h1>
                HOME
            </h1>
        </Container>
    );
}